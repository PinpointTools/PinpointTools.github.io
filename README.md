<div align="center">
    <img src="assets/icons/PT.svg" width=150 height=150 alt="Pinpoint Tools Logo"/>
    <h1>Pinpoint Website</h1>
    <p> The official website repository for the Pinpoint Website. </p>
</div>

<h2> How to run it? </h2>
<p> First of all, you need <code>pnpm</code>. To install <code>pnpm</code>, just do <code>npm install -g pnpm</code> and make sure you have NodeJS installed. </p>

<p> Next, you're gonna have to clone the repository and navigate in. Once you're in, do <code>pnpm install</code>, and then do <code>pnpm dev</code>. This will run the server in <code>localhost:5173</code>. </p>

<h2> How to build it? </h2>
<p> Same folder again, but run <code>pnpm build</code>. Which will trigger vite's build process and compiles it all to one folder, ready to use. </p>

<p> Once it's done downloading, check it first by doing <code>pnpm preview</code> and going to a browser with URL <code>localhost:4173</code>. </p>
